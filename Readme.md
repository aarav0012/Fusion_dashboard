# Steps for setup:
-  Fork and clone the repository [git clone url].
- `cd client`
- `npm install`
- `npm run dev`

# Contributing:
- `git status`
- `git add .`
- `git commit -m "commit message"`
- `git push`
- Open Pull Request from fork